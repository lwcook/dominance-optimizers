from setuptools import setup

setup(name='dominance-optimizers',
        version=0.1,
        author='Laurence W. Cook',
        author_email='lwc24@cam.ac.uk',
        install_requires=['numpy >= 1.12.1'],
        license='MIT',
        packages='domopt')
